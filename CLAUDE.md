# MyCompany

開発会社「MyCompany」のリポジトリです。Claude Code を部署単位のサブエージェントで運用するための構成を含みます。

## リポジトリ構成

```
.
├── CLAUDE.md              # このファイル(Claude Code 向けのプロジェクトガイド)
├── README.md
└── .claude/
    └── agents/            # 部署単位のサブエージェント定義
        ├── frontend-dev.md    # フロントエンド開発部
        ├── backend-dev.md     # バックエンド開発部
        ├── infra-sre.md       # インフラ・SRE部
        ├── qa-testing.md      # 品質保証(QA)部
        ├── security.md        # セキュリティ部
        ├── ui-ux-design.md    # デザイン部(UI/UX)
        ├── pm-planning.md     # PM・企画部
        └── tech-writer.md     # ドキュメント・テクニカルライティング部
```

## サブエージェントの使い方

各サブエージェントは `.claude/agents/*.md` に定義されており、Claude Code がタスク内容に応じて自動的に委譲するか、次のように明示的に呼び出せます。

```
> frontend-dev サブエージェントを使って、ログイン画面のコンポーネントを作成して
> qa-testing サブエージェントでこのPRのテスト観点をレビューして
```

### 部署一覧と担当領域

| サブエージェント | 部署 | 主な担当 |
|---|---|---|
| `frontend-dev` | フロントエンド開発部 | UI実装、コンポーネント設計、状態管理、パフォーマンス |
| `backend-dev` | バックエンド開発部 | API設計・実装、DB設計、ビジネスロジック |
| `infra-sre` | インフラ・SRE部 | IaC、CI/CD、コンテナ、監視、信頼性 |
| `qa-testing` | 品質保証(QA)部 | テスト設計、自動テスト、品質レビュー |
| `security` | セキュリティ部 | 脆弱性診断、セキュアコーディング、依存関係の監査 |
| `ui-ux-design` | デザイン部 | UI/UX設計、デザインシステム、アクセシビリティ |
| `pm-planning` | PM・企画部 | 要件定義、タスク分解、見積もり、進行管理 |
| `tech-writer` | ドキュメント部 | README、設計書、APIドキュメント、リリースノート |

## 開発ルール

- **言語**: コミュニケーションおよびドキュメントは日本語を基本とする。コード内の識別子は英語。
- **コミットメッセージ**: 変更内容が分かる簡潔な日本語または英語で記述する。
- **ブランチ運用**: `main` を保護ブランチとし、作業は機能ブランチで行いPR経由でマージする。
- **レビュー**: 実装後は `qa-testing`、セキュリティ上重要な変更は `security` のレビューを通すことを推奨。

## 部署間の連携フロー(推奨)

1. `pm-planning` が要件を整理しタスクに分解する
2. `ui-ux-design` が画面・体験を設計する
3. `frontend-dev` / `backend-dev` が実装する
4. `infra-sre` がデプロイ・運用基盤を整える
5. `qa-testing` がテストし、`security` がセキュリティ観点を確認する
6. `tech-writer` がドキュメントを整備する
